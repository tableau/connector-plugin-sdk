[DatasourceRegistry]
#These are ordered so that the slower databases come first. That gives them more time to run while other threads can run the faster ones.
#all = *
standard = teradata, netezza
all_passing = standard, bigquery, exasolution
all_test = hadoophive2_hortonworks, standard, all_passing
all_test2 = all_test, all_passing

